## OpenAI-compatible local servers

ZeroCode can talk to any server that supports:

- `GET /models`
- `POST /chat/completions`

LM Studio works out of the box on `http://127.0.0.1:1234/v1`. Other runtimes can be added with the OpenAI-compatible preset.
