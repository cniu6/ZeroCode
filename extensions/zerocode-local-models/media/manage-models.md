## Model manager

The chat model manager is where you:

- enable or disable provider groups
- choose the default chat model
- edit per-model settings like `temperature`, `topP`, and `maxTokens`

If a server does not expose model discovery, fill in the provider group's `models` field manually.
